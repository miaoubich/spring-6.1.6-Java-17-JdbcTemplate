/*
package com.miaoubich.mapper;

import com.miaoubich.dto.ContactInfoRequest;
import com.miaoubich.dto.ContactInfoResponse;
import com.miaoubich.model.ContactInfo;
import org.modelmapper.ModelMapper;

public class ContactInfoMapper {

	private final ModelMapper mapper = new ModelMapper();

	public ContactInfo toEntity(ContactInfoRequest request) {
		return mapper.map(request, ContactInfo.class);
	}

	public ContactInfoResponse toResponse(ContactInfo contactInfo) {
		return mapper.map(contactInfo, ContactInfoResponse.class);
	}
}
*/
